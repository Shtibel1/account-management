{
  "name": "invoice-processor-apps",
  "private": true,
  "workspaces": ["api", "web", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w web\" \"npm run dev -w api\"",
    "build": "npm run build -w packages/shared-types && npm run build -w web && npm run build -w api",
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run dev -w api"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.5.0"
  }
}
